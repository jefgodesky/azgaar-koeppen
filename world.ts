import { createWorld } from './types/World.ts'

/**
 * This provides the world that is actually used by scripts. See
 * ./types/World.ts for documentation on the options that you can pass as part
 * of the object given to `createWorld` to customize your world.
 */

export default createWorld({})
